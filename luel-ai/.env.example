# Database Configuration
# Copy this file to .env and adjust values if needed
DATABASE_URL=postgresql://luel_user:luel_password@localhost:5432/luel_esign

# Individual connection parameters (alternative to DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=luel_esign
DB_USER=luel_user
DB_PASSWORD=luel_password
